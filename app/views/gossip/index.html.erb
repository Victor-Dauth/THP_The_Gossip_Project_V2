<h1>Bienvenu sur la page d'accueil du site THE GOSSIP PROJECT</h1>
<h3>Voici la liste des gossip sur notre site :</h3>


<div class="row">
  <% @gossips.each do |g| %>

    <div class="card text-white bg-dark mb-3 col-sm-6" style="max-width: 18rem; margin-left: 0.5em; margin-right: 0.5em;">
      <div class="card-header">
        <%= link_to "#{g.title}", gossip_path(g.id) %>
      </div>
      <div class="card-body">
        <blockquote class="blockquote mb-0">
          <p> <%= g.content %> </p>
          <footer class="blockquote-footer">De : <cite title="Source Title"> <%= link_to "#{g.user.first_name}", user_path(g.user.id) %> </cite></footer>
        </blockquote>
      </div>
      <div class="card-footer text-muted">
        <%= g.created_at %>
      </div>
    </div>
  <% end %>
</div>
